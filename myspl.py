# import pymysql

# config = {'host':'localhost',
#           'user':'root',
#           'password':'',
#           'db':'portfolio_db,
#           charset':'utf8'}

# con = pymysql.connect(**config)
# with con.cursor() as cursor:
#     sql = '''CREATE TABLE customer_table (id int name varchar(20) tel int message text)'''
#     cursor.execute(sql)

# con.commit()
# con.close()